<style lang="less">
  .hello{}
</style>
<template>
    <<%= ProjectName %> />
</template>

<script>
  import wepy from 'wepy';
  import <%= ProjectName %> from '../components/index';

  export default class extends wepy.page {

    config = {
      navigationBarTitleText: '<%= project_name %> demo'
    }

    components = {
      <%= ProjectName %>
    }

    data = {
    }

    computed = {
      now () {
        return +new Date()
      }
    }

    methods = {
    }
  }
</script>
